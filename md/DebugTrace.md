# Debug & Trace

- in `application.properties`
  - logging.level.org.springframework.security=TRACE

- in `application.yml`
```agsl
  logging:
    level:
      org:
        springframework:
          security: DEBUG
```